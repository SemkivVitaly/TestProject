
# Nested repo (we push from root)
_New/esp32s3box_wifi_bridge/esp32s3box_wifi_bridge/.git

# PlatformIO
.pio/
**/.pio/
*.dblite

# BrigeLogCopy (.NET): obj/.vs не в репо; bin/Debug — отслеживаем (готовый exe в репозитории).
BrigeLogCopy/obj/
BrigeLogCopy/.vs/

Thumbs.db
